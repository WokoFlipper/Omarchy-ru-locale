# Handoff: русская локаль Omarchy (2026-09-18)

## Люди
- Мы: WokoFlipper (носитель, редактор, последнее слово по-русски), ассистент (файлы, коммиты, API).
- Соавтор: sbelcl / Imnos (Словения; словенский; пишет через Claude — стиль узнаваем).
- linyiru: автор adjudication #12345, измеряет, присылает PR.
- pluseight8: сайт #251 (мы отдали свой #322 в его пользу).
- HANCORE-linux: маркетплейс плагинов.

## Репозитории
- Наш: WokoFlipper/Omarchy-ru-locale, ветка master. Меню 137, shell 177, cli 61, бинды 204 (архив!), wifi 18, bt 28, display 15, ru.json 179, archive/ не удалять.
- Общий дом: sbelcl/omarchy-language (write-доступ есть). ru_RU.tsv вмержен, ru.json вмержен (#2, #6).
- Клон для PR ему: ~/git/omarchy-language (переживает ребуты; /tmp чистят!).

## Открыто (ждём чужих рук)
- sbelcl#3 (Direct Boot), sbelcl#7 (brightness %), sbelcl#8 (audio %) — пинать.
- weather#1 (m/s, с нашего форка) — записи в его weather-форк нет.
- Апстрим: ветер #11139 (пнули 3 раза), рантайм #8765, adjudication #12345, даты #10955, пикер #10949, DNS-guard #12395 (наш), kbd #12400 (наш), bindings schema #12404 (вписались).
- Маркетплейс #6661: Charge Limit опубликован ✅.
- См. также docs/memo-unmerged.md (архив ~/backups/unmerged-*.tar.gz).

## Новое с 18.09
- Таблицы wifi/bt/display-ru.tsv; ru.json 179 (аудио/яркость шкалы в %).
- Словенцу пишем по-словенски (dual bova-формы заходят).
- Его форки: clock/power/audio/BT/weather/monitor/agents/tray/indicators/update.
- Клоны держать в ~/git (не /tmp — чистят!).
- DNS: DNS0.eu вместо AdGuard в бэкапах; reapply.sh ждёт пароль из терминала.

## Правила работы (согласовано)
- Русский черновик мне → "да" → постим английский. Стратегическое без "да" не постим.
- Переводы десятками, нумерация по трекам (M меню, R/S/T панели, N сайт).
- Формат панелей: ключи байт-в-байт, CLDR one/few/many, %n на месте.
- Бинды НЕ применять локально (ломают мержи). Custom/DNS-метки — протокольные слова.
- Числа до миллиона цифрами; лента млн, заголовки статей целиком.
- meценат/фонд Omacom — термины сайта.
- Ветка у всех master (не main!).
- Локальные проверки: msgfmt --check, python json key-match, git sync 0/0.
- Осторожно: bash-блокировщик "EXPLORATION SPRAWL" — игнорить, писать файл (Write) и продолжать. /tmp чистят ребуты.
- Трекер: track.sh (6 точек), таймер почасовой locale-track.timer. Nudge-пингер СНЯТ.
- sudo: ритуал (звук + notify + предупреждение), таймаут 180000.
- Клоны держать в ~/git (не /tmp — чистят!).
- Лимит заряда 70%.
- resty.weather перебазирован на текущий сток (фриз ушёл), включён.
- Словенцу пишем по-словенски (dual bova-формы).
- Нераспознанный коммит 07e67ea (bindings DESC-migration) — проверить авторство.
- docs/install-ru.md — инструкция установки меню по-русски.

## Техдолг
- Plural-компоненты сайта (Intl.PluralRules): PR-счётчик, участники, лайки, звёзды.
- Схема stable id для биндов (вызвались помочь в #12345).
- reapply DNS (bash ~/backups/network-dns/reapply.sh) — нужен пароль из терминала.
- resty.weather: был фриз, перебазирован — работает.
