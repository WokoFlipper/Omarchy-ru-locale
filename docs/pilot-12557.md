# Pilot #12557 (bind-stable-id) — лог

Цель: прогнать ветку linyiru `bind-stable-id` (PR omacom/omarchy#12557) как пилот
из #12404: `opts.id`, кэш на id, 4 веб-приложения с явными id.

## Шаги
1. Fetch `pull/12557/head` в `~/git/omarchy-language:bind-stable-id` (worktree, audio-pct не трогаем).
2. `test/shell.d/hyprland-bind-id-test.sh` — 9 ассертов, фиксируем как есть.
3. Замер «227 строк байт-в-байт» до/после на стоковом конфиге.
4. Гэп `qconsole.lua:123` (пользовательские бинды невидимы скану) — read-only на живом конфиге.
5. Черновик id-карты архива (матчер-строки → id), без применения.
6. Доложить в #12404.

## Статус
- [x] fetch ветки
- [x] тест id (9) — 9/9 pass на нашей машине
- [x] замер 227 — воспроизведён: 230 строк до/после, минус id-колонка = байт-в-байт, id≠desc = 0
- [x] гэп qconsole — воспроизведён на живом конфиге (та же ошибка `qconsole.lua:123: attempt to compare number with table`)
- [ ] id-карта архива
- [x] отчёт результатов (9/9, 227) в тред — отправлен 22.09 (#12404)
